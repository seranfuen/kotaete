@using Resources
@model List<KotaeteMVC.Models.QuestionDetailAnswerViewModel>

@if (Model.Count > 0)
{
    foreach (var question in Model)
    {
        Html.RenderPartial("InboxQuestion", question);
    }
}
else
{
    Html.RenderPartial("NoAnswers");
}
